# Example Terraform variables file
# Copy this to terraform.tfvars and fill in your values

# GitHub App installation ID
# Get this from: Cloud Console > Cloud Build > Connections
# After creating a connection, the installation ID will be shown in the connection details
# Or verify with: gcloud builds connections describe CONNECTION_NAME --region=us-west1 --format="value(githubConfig.appInstallationId)"
github_app_installation_id = "YOUR_INSTALLATION_ID"

# GitHub OAuth token secret version
# Get this from: gcloud builds connections describe CONNECTION_NAME --region=us-west1 --format="value(githubConfig.authorizerCredential.oauthTokenSecretVersion)"
# Format: projects/PROJECT/secrets/SECRET/versions/VERSION
github_oauth_token_secret_version = "projects/k0sngin/secrets/k0sngin-github-oauthtoken-6cb962/versions/latest"
